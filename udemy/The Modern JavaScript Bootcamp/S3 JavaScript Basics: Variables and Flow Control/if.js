// === - equality operator
// !== - not equal operator
// < - less than operator
// > - greater than operator
// <= - less than or equal operator
// >= - greater than or equal operator

temp = 110;

if (temp <= 32) {
  console.log("It's freezing outside!");
}

if (temp >= 110) {
  console.log("It's way too hot outside!");
}

// Challenge Area

// Create age set to your age
let myAge = 65;
// If age is less than or equal to 7
if (myAge <= 7) {
  // Print the child discount
  console.log("You are going to get a child discount");
}
// If age is greater than or equal to 65
if (myAge >= 65) {
  // Print the senior discount
  console.log("You are going to get a senior discount");
}
